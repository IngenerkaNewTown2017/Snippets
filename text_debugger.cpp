#include <string>

/* std::string — класс с методами и переменными для организации работы
 * со строками в языке программирования C++. Он включён в стандартную
 * библиотеку C++. Название образовано от имени строчного типа данных
 * (англ. string). Являясь частью стандартной библиотеки C++,
 * эти объекты также являются частью стандартного пространства имён — std.
 */

txTextOut(100, 100, std::string(car_x_coord).c_str());
